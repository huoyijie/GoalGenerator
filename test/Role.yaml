package: auth
name: Role
storageRules:
    - embeddingBase
componentRules:
    - lazy
fields:
    - name: Name
      storageRules:
        - unique
      component: text
      componentRules:
        - sortable
      validateRules:
        - required
        - alphanum
        - '@min=3'
        - '@max=40'
